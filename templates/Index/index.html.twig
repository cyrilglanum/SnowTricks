{% extends 'base.html.twig' %}

{# {% block title %}Hello BlogController!{% endblock %} #}

{% block body %}

    <div class="example-wrapper">

        {% for message in app.flashes('success') %}
            <div class="alert alert-success" role="alert">{{ message }}</div>
        {% endfor %}

        <img src="{{ asset('snow2.jpg') }}" style="width: 100%; height:900px">

        <div class="container">
            <div class="col-12">
                <div class="row">


                    {% for trick in tricks %}
                        <div class="col-3">
                            {{ trick.name }}
                            {{ trick.description }}
                            <br>
                            <img src="{{ asset('uploads/tricksFile') }}/{{ trick.imgBackground }}">
                            {# {{ trick.date_creation }} #}
                            <em>{{ trick.dateCreation }}</em>
                            <hr>
                        </div>
                    {% endfor %}

                </div>

            </div>
        </div>
    </div>



{% endblock %}
